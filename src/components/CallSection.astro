<section id="call" class="call section section--surface" aria-labelledby="call-heading">
  <div class="container call__inner">
    <div>
      <h2 id="call-heading" class="headline call__title">Prefer to talk first?</h2>
      <p class="call__lead">
        Some customers want to walk through their space on the phone before
        booking. We are glad to — same team, same transparent pricing either way.
      </p>
    </div>
    <div class="call__card">
      <p class="call__label">Phone number</p>
      <p class="call__number mono" aria-label="Phone number coming soon">
        (407) ___-____
      </p>
      <p class="call__note">
        Final number coming soon. Request a quote above for the fastest response
        in the meantime.
      </p>
      <a class="btn btn--primary" href="/book">Get a free quote instead</a>
    </div>
  </div>
</section>

<style>
  .call__inner {
    display: grid;
    gap: var(--space-10);
    align-items: center;
  }

  .call__title {
    margin: 0 0 var(--space-3);
    font-size: clamp(1.5rem, 3vw, 2rem);
    color: var(--teal-deep);
  }

  .call__lead {
    margin: 0;
    max-width: 42ch;
    color: var(--ink-secondary);
  }

  .call__card {
    padding: clamp(var(--space-6), 4vw, var(--space-10));
    background: var(--ground);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
  }

  .call__label {
    margin: 0 0 var(--space-2);
    font-size: 0.8125rem;
    font-weight: 600;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--ink-secondary);
  }

  .call__number {
    margin: 0 0 var(--space-3);
    font-size: clamp(1.75rem, 4vw, 2.25rem);
    font-weight: 600;
    color: var(--teal-deep);
  }

  .call__note {
    margin: 0 0 var(--space-6);
    color: var(--ink-secondary);
    font-size: 0.9375rem;
  }

  @media (min-width: 768px) {
    .call__inner {
      grid-template-columns: 1.2fr 1fr;
    }
  }
</style>
